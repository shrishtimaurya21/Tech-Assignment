#include <iostream>
using namespace std;
int main()
{
    int number;
    cout << "Enter a number: ";
    cin >> number;
    int sum = 0;
    while (number != 0)
    {
        sum += number % 10;
        number /= 10;
    }
    cout << "The sum of the digits is: " << sum << endl;
    return 0;
}
