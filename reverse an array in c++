#include<bits/stdc++.h>
using namespace std;
int main(){
   int arr[] = {13, 24, 35, 46, 57};
   int n = sizeof(arr)/sizeof(arr[0]);
   int start = 0, end = n-1;
    while(start < end){
        swap(arr[start++], arr[end--]);
    }
    for(int i=0; i<n; i++)
       cout << arr[i] << " ";
}
